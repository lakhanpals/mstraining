spring:
  application:
    name: configserver
  cloud:
    config:
     server:
       git:
        uri: c:/mstraining-config

management:
  endpoints:
    web:
      exposure:
        include: '*'
server:
  port: 8888  